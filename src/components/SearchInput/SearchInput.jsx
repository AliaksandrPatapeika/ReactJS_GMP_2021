import './SearchInput.less';

import React from 'react';

const SearchInput = () => (
  <input className="searchInput" placeholder="What do you want to watch?" autoComplete="off" />
);

export default SearchInput;
